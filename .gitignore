.claude/settings.local.json
.claude/skills/*
.DS_Store
*/.DS_Store
# One rule instead of one line per module: the per-module list silently failed to cover
# :stockfish-assets when it was added, and an AGP-generated asset-pack manifest landed in git.
**/build/
.kotlin/*
.gradle/*
.idea/*
.zcode/
opencode.json
.mcp.json
.superpowers/
.worktrees/
local.properties

iosApp/iosApp.xcodeproj/xcuserdata
iosApp/iosApp.xcodeproj/project.xcworkspace/xcuserdata
iosApp/build/*

# JVM crash logs
app/hs_err_pid*.log
hs_err_pid*.log
app/app/

# Issue #54 — Filament iOS: fetched static libs/headers + staged IBL (tools/fetch_filament_ios.sh)
iosApp/iosApp/Filament/filament/
iosApp/iosApp/Resources/papermill_ibl.ktx
iosApp/iosApp/Resources/papermill_skybox.ktx

# Desktop Filament: fetched native release payload + bridge build output
app/src/desktopMain/filament/filament/
app/build/desktop-filament-native/

# Filament cmgen/CLI tools downloaded to bake/blur the KTX environment maps (binaries, not source)
/filament/
iosApp/iosApp/Resources/papermill_skybox_blurred.ktx

# Opening-coach server: MiniLM ONNX model + vocab fetched for local runs.
# server/Dockerfile downloads these at build time from a pinned HF revision.
server/model/
server_logs.txt
# Excluded artifacts
DownloadTest.kt

# Local LiteRT-LM faithfulness driver output (:litert-eval), scored out-of-band by ferryman
litert-outputs*.json

# Agent scratch output: copied JUnit XML, scraped logs, sampled generations, ad-hoc patch scripts,
# and the GRADLE_USER_HOME some runs point at the repo root. All of it was committed once by
# accident; none of it is an input to any build.
.gradle_home/
TEST-*.xml
samples.txt
samples-*/
binary/
fly_logs.txt
diff.txt
pr_body.md
