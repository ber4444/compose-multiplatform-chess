{
  "name": "chess3d-renderer-build",
  "version": "0.169.0",
  "private": true,
  "description": "Bundles the shared three.js chess renderer for iOS and regenerates the wasm inline string. Run: npm install && node build.mjs",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "esbuild": "^0.23.1",
    "three": "0.169.0"
  }
}
