## Goal

Get PR #88 out of draft on the merits the user named — it's permanent, provider-agnostic test infrastructure — by stripping the now-moot GLM/z.ai framing from surface artifacts. **No logic changes.** The production code (`Composers.kt`, `Application.kt`, `EvalMain.kt`) is already provider-neutral (defaults point at `openai.com`/`gpt-4.1-mini`; all config from env). All 7 tests pass (`./gradlew :server:test` → BUILD SUCCESSFUL, verified this session). The GLM motivation is dead: ferryman-mcp #10 (merged 2026-07-17) ranked zai-glm last on company-research (68%) and tied-last on chess (52%) at ~10× gemini's cost and ~24× its latency.

## Scope — surface artifacts only

The z.ai/GLM footprint lives entirely in five places; each gets reframed, none changes behavior.

### 1. Commit: rewrite `docs/plans/zai-glm-provider-addendum.md` (rename + content)
- **Rename** to `docs/plans/llm-composer-provider-infra.md` (cleaner title; `git mv` preserves history).
- **Rewrite to provider-neutral framing:**
  - Title: "Plan: Provider-shaped `LlmComposer` + eval route".
  - Context: drop "the repo owner has z.ai API credits" / "GLM the natural first concrete provider". Reframe as: the composer and eval matrix need *a* concrete provider shape; this plan makes the composer testable and adds an eval row.
  - Deadline section: replace the July-19 credit-urgency sequencing with a **note that M2 was answered cross-repo**: ferryman-mcp #10 (2026-07-17) ran the multi-provider matrix and ranked z.ai's GLM last on both skills (company-research 68%, chess 52%) at ~10× gemini's cost and ~24× its latency — so there is no eval case for GLM as this app's composer; the composer ships provider-neutral and any future provider is a config change. Keep the "after-expiry / swap config" paragraph but generalize it (drop the GLM open-weight preservation wording).
  - Hard rules: keep all five verbatim (they're provider-agnostic already — "keys/URLs from env only", "default stands", "budget enforced", "judge never grades own family", "read live docs first" generalized to "the chosen provider"). The `COACH_LLM_BASE_URL` env var names stay (already neutral).
  - M1 section: drop "(GLM-backed)" from the heading; drop "Ktor RAG + GLM compose" → "LLM-composed"; mark M1's checkboxes done.
  - M2/M3 sections: annotate as **answered cross-repo** by ferryman #10, with the one-line verdict, rather than "remaining".
  - Article hooks: drop (no template-vs-GLM comparison to write; the verdict closed that).
- **Note:** the test file's KDoc references this doc by old filename (`docs/plans/zai-glm-provider-addendum.md M1`); update that line in step 3.

### 2. Commit: de-Glm the code comments + test fixture (no behavior change)
- `server/src/main/kotlin/com/example/coachserver/Application.kt:134` — `selectComposer` KDoc: change "provider-shaped, not z.ai-shaped" → "provider-shaped (OpenAI-compatible), not tied to any vendor".
- `server/src/test/kotlin/com/example/coachserver/LlmComposerHttpTest.kt`:
  - Line 15 class-header KDoc: change "required by docs/plans/zai-glm-provider-addendum.md M1" → "required by docs/plans/llm-composer-provider-infra.md M1".
  - Line 45/59 fixture: change `"glm-4.6"` → `"gpt-4.1-mini"` (matches the production default; neutral). The `assertTrue(sent.contains("\"model\":\"gpt-4.1-mini\""))` assertion still proves request serialization round-trips the model name — same coverage.
- `docs/plans/` is the only place "z.ai"/"glm" remain in the changed area after this, and step 1 rewrites those.

### 3. Commit: `evals/scorecard.md` — no change needed
The new row `local-llm-compose | optional (COACH_LLM_API_KEY or token prices not set)` is already provider-neutral. Leave it.

### 4. Mark PR ready-for-review
- **Retitle:** `z.ai/GLM provider: LlmComposer tests + eval route (M1)` → `LlmComposer: fake-HTTP-engine tests + optional eval route (M1)` via `gh pr edit 88 --title ...`.
- **Rewrite body** to lead with the merits the user named, not the dead GLM motivation:
  - Lead: "Permanent, provider-agnostic LlmComposer test infrastructure + an optional eval route. Nothing z.ai-specific is committed."
  - **What changed (the four permanent pieces):** `LlmHttpTransport` fake-HTTP-engine (fun interface, no mocking lib); `selectComposer()` extraction (env-gating unit-testable without a DB); the 7 tests including budget-enforcement-before-transport; the optional `local-llm-compose` eval route.
  - **Why this left draft:** the GLM motivation is moot — cross-link ferryman-mcp #10 with the one-line verdict (zai-glm last/tied-last at ~10× cost, ~24× latency on chess). State the July-19 deadline point bluntly: soft, because M2 already happened cross-repo and glm-4.5-flash/glm-4.7-flash are free-tier so the manual e2e request doesn't die with the credits — it just moves to a free model. Conclusion: land on the infra's merits; the composer stays provider-neutral for any future provider.
  - **Hard rules honored** (kept from original, all verifiable from the diff): nothing provider-specific committed; `TemplateComposer` default stands; cost budget enforced (`maxUsdCents = 0.2` → hard token cap, refuses + falls back); move-coach decider tests untouched (`LOCAL_ONLY` can't route to `Cloud`).
  - **Verification:** `./gradlew :server:test` → all green (15 existing + 7 new = 22).
- `gh pr ready 88` to take it out of draft.
- Post a short review comment summarizing the reframe for any reviewer who saw the old draft.

## Out of scope (explicitly)
- No `Composers.kt` / `Application.kt` / `EvalMain.kt` **logic** changes — already provider-neutral; tests green.
- No article hooks (the template-vs-GLM comparison is dead with the verdict).
- No M2/M3 follow-ups opened here — answered cross-repo.
- Two unrelated pre-existing `Z.AI` mentions (`issue-39-game-lifecycle-persistence.md`, `3d-parity-desktop-bloom-web-aa.md`) are "Z.AI the *agent audience*", not the provider — leave them.

## Verification before un-drafting
1. `./gradlew :server:test` → expect 22 tests green (already confirmed on current branch; re-run after the doc/comment edits to be certain nothing regressed — they're docs/comments only, but confirm).
2. `grep -rniE "z\.ai|zai|glm" --include="*.kt" server/ evals/` → expect only `gpt-4.1-mini`-neutral fixtures, no vendor leakage.
3. `grep -rniE "glm" docs/plans/llm-composer-provider-infra.md` → expect zero, or only inside the ferryman #10 verdict sentence where it's the *subject* of the finding, not the recommendation.

## Order of operations
1. Edits to `docs/plans/` (rename + rewrite), `Application.kt` comment, `LlmComposerHttpTest.kt` KDoc + fixture string.
2. One commit: `refactor(server): land LlmComposer provider-infra as provider-neutral (drop moot GLM framing)`.
3. Push to `feat/zai-glm-provider`.
4. Re-run `./gradlew :server:test`, confirm green.
5. `gh pr edit 88 --title ...` + body rewrite + `gh pr ready 88`.
6. Optional reviewer-context comment.