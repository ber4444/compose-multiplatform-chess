diff --git a/.gitignore b/.gitignore
index 2df9033..7048613 100644
--- a/.gitignore
+++ b/.gitignore
@@ -19,3 +19,8 @@ app/libWGPU-*.dylib
 app/hs_err_pid*.log
 hs_err_pid*.log
 app/app/
+
+# Issue #54 — Filament iOS: fetched static libs/headers + staged IBL (tools/fetch_filament_ios.sh)
+iosApp/iosApp/Filament/filament/
+iosApp/iosApp/Resources/papermill_ibl.ktx
+iosApp/iosApp/Resources/papermill_skybox.ktx
diff --git a/CLAUDE.md b/CLAUDE.md
index c51d458..076cf8f 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -95,7 +95,7 @@ Stockfish binaries are vendored at `app/src/androidMain/jniLibs/{arm64-v8a,armea
 - **3D Board View:** The `GameScreen` settings row shows a 3D toggle (`viewState.show3D`), but only when a `Board3DSupport` is injected (desktop, web, iOS, and Android entry points pass their respective `Board3DSupport`; unsupported platforms pass `null`, hiding the toggle). Toggling **swaps** the board: exactly one board (2D `chess_board` or 3D `board_3d`) is visible at a time. The 3D view is defined by the `Chess3DBoardRenderer` interface in `commonMain`, injected mirroring `ChessEngine`. `Board3DSceneMapper.fromFen` turns a FEN into a renderer-agnostic `Board3DScene`. Interaction (camera drag, pinch zoom, ray-picked tap-to-move, and piece transitions) uses shared commonMain logic (`OrbitCameraController`, `CameraMath.rayFromScreen`, `BoardRayPicker`, `Board3DSceneDiffer`) so backend cameras stay in sync with the picker.
   - **Desktop backend:** **Vulkan (LWJGL)** with the full vkChess PBR pipeline. `VulkanChessRenderer` renders offscreen into a `VK_FORMAT_R8G8B8A8_UNORM` color target, copies to host memory, and hands the RGBA pixels to the attached `ImageBitmapChess3DSurface` as a Compose `ImageBitmap`. The pipeline: Cook-Torrance direct lighting (D_GGX + G_SchlicksmithGGX + F_Schlick) modulated by a 2048² PCF shadow pass, precomputed irradiance cube + BRDF LUT + Filament's prefiltered `papermill_ibl.ktx` for IBL, per-pixel metallic/roughness from the glTF MR texture, tangent-space normal mapping (glFT tangents + `marble-speckled-normal`), per-material push constants (baseColorFactor/metallicFactor/roughnessFactor), warm sunlight + warm-tinted ambient, Uncharted2 filmic tonemap + gamma. Quality knobs: SSAA supersampling (`CHESS_DESKTOP_SSAA`, default 2×) + 8× MSAA + 16× anisotropic. Shaders are GLSL compiled to SPIR-V at runtime via shaderc.
   - **Web backend:** **three.js** (WebGLRenderer + MeshStandardMaterial + ACESFilmicToneMapping + PCFSoftShadowMap). `ThreeJsChessRenderer` injects three.js + a chess-renderer module (inline ES module resolved via a static import map in `index.html`) into the page, loads `chess.glb` via three's `GLTFLoader`, and renders into an absolutely positioned overlay `<canvas>`. Kotlin drives camera, FEN, and selection via `@JsFun` interop through the same `Chess3DBoardRenderer` contract. (The Phase B spike matched/exceeded Android's per-frame quality — see `docs/plans/web-graphics-spike-result.md`, verdict: ADOPTED.)
-  - **iOS backend:** **three.js via WKWebView** (same renderer as web). `WKWebViewChessRenderer` loads a self-contained esbuild bundle (`chess3d-bundle.js` = three.js r169 + GLTFLoader + RoomEnvironment + chess-renderer code, fully offline, no CDN) alongside `chess.glb` from the app bundle. The WKWebView sits inside a `UIKitView(interactive = false)` so Compose `pointerInput` intercepts touches and the shared `OrbitCameraController` / `BoardRayPicker` pipeline drives the camera via `evaluateJavaScript`. RealityKit was evaluated and rejected — ModelIO can't load `.glb`, USDZ conversion loses materials, and even a native Sketchfab `.usdz` rendered as flat gray due to RealityKit's limited `UsdPreviewSurface` support (see `docs/plans/ios-graphics-spike-result.md`).
+  - **iOS backend:** **three.js via WKWebView** (same renderer as web). `WKWebViewChessRenderer` loads a self-contained esbuild bundle (`chess3d-bundle.js` = three.js r169 + GLTFLoader + RoomEnvironment + chess-renderer code, fully offline, no CDN) alongside `chess.glb` from the app bundle. The WKWebView sits inside a `UIKitView(interactive = false)` so Compose `pointerInput` intercepts touches and the shared `OrbitCameraController` / `BoardRayPicker` pipeline drives the camera via `evaluateJavaScript`. RealityKit was evaluated and rejected — ModelIO can't load `.glb`, USDZ conversion loses materials, and even a native Sketchfab `.usdz` rendered as flat gray due to RealityKit's limited `UsdPreviewSurface` support (see `docs/plans/ios-graphics-spike-result.md`). An **optional Metal-native Filament backend** exists behind a build flag (`FilamentIosChessRenderer`, the `Debug-Filament` config / `iosApp-Filament` scheme): a Swift `FilamentChessView` (CAMetalLayer + CADisplayLink) conforms to the Kotlin `FilamentChessNativeView` protocol and is injected into `MainViewController(engine, filamentFactory)` — the default build stays on WKWebView and never links Filament. Requires `tools/fetch_filament_ios.sh` (fetches Filament v1.72.0 xcframeworks; both device + arm64-simulator slices, so it runs in the iPhone simulator on Apple Silicon). Verified rendering the full board on the iPhone 17 simulator; see `docs/plans/ios-filament-spike-result.md` (issue #54). Still pending: 120 fps on a ProMotion device + look fine-tuning.
   - **Android backend:** A Filament renderer via **SceneView** (`io.github.sceneview:sceneview`), the Jetpack-Compose-native Filament wrapper (`AndroidSceneViewChessRenderer`). The renderer is a Compose-observable state holder behind `Chess3DBoardRenderer`; `AndroidBoard3DSurface` hosts SceneView with `SurfaceType.Surface`, papermill IBL/skybox KTX assets, and a transparent Compose overlay that receives the shared gestures because SceneView consumes touches. SceneView's model loader loads the `chess.glb` asset and fixed `ModelNode` pools render the board, pieces, and selected-square highlight. This replaced the original hand-written Filament `Engine`/`Renderer`/`SwapChain` + `SurfaceHolder.Callback` plumbing; Materia and a raw NDK Vulkan renderer were evaluated and rejected (see `docs/plans/issue-32-3d-ui-m3-android.md` and `docs/plans/issue-32-3d-ui-unresolved-questions.md`).
 
 ## Build quirks (don't "clean up")
diff --git a/app/src/iosMain/kotlin/com/example/myapplication/MainViewController.kt b/app/src/iosMain/kotlin/com/example/myapplication/MainViewController.kt
index 72aecae..98911bf 100644
--- a/app/src/iosMain/kotlin/com/example/myapplication/MainViewController.kt
+++ b/app/src/iosMain/kotlin/com/example/myapplication/MainViewController.kt
@@ -12,14 +12,27 @@ import androidx.compose.ui.unit.dp
  * iOS entry point. The engine is created and started on the Swift side
  * (StockfishChessEngine) and injected here, mirroring desktop Main.kt.
  * Pass null to play against the built-in CPU.
+ *
+ * [filamentFactory] selects the 3D board backend: pass a Swift `FilamentChessFactory` (gated behind
+ * the `FILAMENT_ENABLED` build config) to use the Metal-native Filament renderer, or `null` for the
+ * default three.js/WKWebView renderer. See `docs/plans/ios-filament-spike-result.md`.
  */
 @OptIn(ExperimentalForeignApi::class)
-fun MainViewController(engine: ChessEngine?): UIViewController = ComposeUIViewController {
+fun MainViewController(
+    engine: ChessEngine?,
+    filamentFactory: com.example.myapplication.board3d.FilamentChessViewFactory? = null,
+): UIViewController = ComposeUIViewController {
     val viewModel = remember { GameViewModel() }
     DisposableEffect(Unit) {
         viewModel.attachEngine(engine)
         if (platform.posix.getenv("CHESS_START_3D") != null) viewModel.setShow3D(true)
         onDispose { viewModel.close() }
     }
-    MyApplicationTheme { ChessApp(viewModel = viewModel, board3D = remember { com.example.myapplication.board3d.iosBoard3DSupport() }, switchTopPadding = (-16).dp) }
+    MyApplicationTheme {
+        ChessApp(
+            viewModel = viewModel,
+            board3D = remember { com.example.myapplication.board3d.iosBoard3DSupport(filamentFactory) },
+            switchTopPadding = (-16).dp,
+        )
+    }
 }
diff --git a/app/src/iosMain/kotlin/com/example/myapplication/board3d/IosBoard3D.kt b/app/src/iosMain/kotlin/com/example/myapplication/board3d/IosBoard3D.kt
index 2c68ff0..d725286 100644
--- a/app/src/iosMain/kotlin/com/example/myapplication/board3d/IosBoard3D.kt
+++ b/app/src/iosMain/kotlin/com/example/myapplication/board3d/IosBoard3D.kt
@@ -66,8 +66,24 @@ private fun mimeForFile(file: String): String = when {
     else -> "application/octet-stream"
 }
 
+/**
+ * iOS 3D board support. Defaults to the three.js/WKWebView renderer ([WKWebViewChessRenderer]).
+ *
+ * When a [filamentFactory] is injected (from Swift, gated behind the `FILAMENT_ENABLED` build
+ * config — see `docs/plans/ios-filament-spike-result.md`), the Metal-native Filament renderer
+ * ([FilamentIosChessRenderer]) is used instead. Both implement [Chess3DBoardRenderer], so the rest
+ * of the app is unaffected by the choice. Passing `null` keeps the shipping WKWebView path.
+ */
 @OptIn(ExperimentalForeignApi::class)
-fun iosBoard3DSupport(): Board3DSupport {
+fun iosBoard3DSupport(filamentFactory: FilamentChessViewFactory? = null): Board3DSupport {
+    if (filamentFactory != null) {
+        return Board3DSupport(
+            rendererFactory = { FilamentIosChessRenderer(filamentFactory) },
+            surfaceContent = { renderer, modifier ->
+                FilamentIosBoard3DSurface(renderer, modifier)
+            }
+        )
+    }
     return Board3DSupport(
         rendererFactory = { WKWebViewChessRenderer() },
         surfaceContent = { renderer, modifier ->
diff --git a/docs/plans/ios-graphics-spike-result.md b/docs/plans/ios-graphics-spike-result.md
index d8972d1..cf15de1 100644
--- a/docs/plans/ios-graphics-spike-result.md
+++ b/docs/plans/ios-graphics-spike-result.md
@@ -77,3 +77,6 @@ would:
   ready-to-use Swift Package Manager integration. The integration cost is real but bounded;
   recorded as a fallback option if three.js via WKWebView hits a blocking limitation in
   production (e.g., touch latency, WebGL2 feature gaps).
+  **Update (issue #54):** that bounded cost was paid — a Metal-native Filament backend is now wired
+  behind a build flag (`Debug-Filament`), with the default build still shipping WKWebView. See
+  `docs/plans/ios-filament-spike-result.md`.
diff --git a/iosApp/iosApp.xcodeproj/project.pbxproj b/iosApp/iosApp.xcodeproj/project.pbxproj
index 71aa561..babe71d 100644
--- a/iosApp/iosApp.xcodeproj/project.pbxproj
+++ b/iosApp/iosApp.xcodeproj/project.pbxproj
@@ -9,12 +9,15 @@
 /* Begin PBXBuildFile section */
 		0BD150F6371E4A26A3D7624D /* StockfishChessEngineTests.swift in Sources */ = {isa = PBXBuildFile; fileRef = BAEBFE68677C845B5A891EB9 /* StockfishChessEngineTests.swift */; };
 		1C81D6F669B7BB6D2FFCCEB3 /* ChessKitEngine in Frameworks */ = {isa = PBXBuildFile; productRef = DB95EAE1EB3A07DF03F63DAF /* ChessKitEngine */; };
+		2C9378ABBEEF946E998A907A /* FilamentChessRenderer.mm in Sources */ = {isa = PBXBuildFile; fileRef = 84E513D241B7AC10D88E4034 /* FilamentChessRenderer.mm */; };
 		307C2654CD5972A25FB7FD87 /* iOSApp.swift in Sources */ = {isa = PBXBuildFile; fileRef = 668CECA83ACC03A83C85A623 /* iOSApp.swift */; };
+		344EA7D190B1CBFC69C6995F /* FilamentChessView.swift in Sources */ = {isa = PBXBuildFile; fileRef = 2F000A1A124DCED79B34ED01 /* FilamentChessView.swift */; };
 		3F4B6FE98FC3A112054A4766 /* StockfishChessEngine.swift in Sources */ = {isa = PBXBuildFile; fileRef = 28C7F05371BE73DC00FFB6B0 /* StockfishChessEngine.swift */; };
 		56B0305B5D3847FC6B326502 /* ContentView.swift in Sources */ = {isa = PBXBuildFile; fileRef = CE8509E5A49E4A5CAAEB3ADB /* ContentView.swift */; };
 		7DAA6B2A3EBDDF2583C94275 /* nn-1111cefa1111.nnue in Resources */ = {isa = PBXBuildFile; fileRef = 0083126833BD37C48CC51E07 /* nn-1111cefa1111.nnue */; };
 		83C131FF4DF845EB044A1205 /* chess.glb in Resources */ = {isa = PBXBuildFile; fileRef = 39DD6E47D4950503102B28AE /* chess.glb */; };
 		8AB4FBE85B9BC0AB0F289E80 /* chess3d-host.html in Resources */ = {isa = PBXBuildFile; fileRef = 7D8DD3253426D5CAAB9B26A6 /* chess3d-host.html */; };
+		AE3AA8C5D37E58F01986EEFC /* FilamentChessFactory.swift in Sources */ = {isa = PBXBuildFile; fileRef = 1C96BB04BA068592D857136E /* FilamentChessFactory.swift */; };
 		B5CA9B9E6C1205698D8723A5 /* nn-37f18f62d772.nnue in Resources */ = {isa = PBXBuildFile; fileRef = DBCCFF2A8437C866DD0AAE79 /* nn-37f18f62d772.nnue */; };
 		BACEF7A54BF3B28F14E29C94 /* papermill_skybox.hdr in Resources */ = {isa = PBXBuildFile; fileRef = 097ADECF758538D6C7CE71CD /* papermill_skybox.hdr */; };
 		D33213E236C27526B84AD847 /* papermill_ibl.hdr in Resources */ = {isa = PBXBuildFile; fileRef = CFAFF076B417B6726E6C948D /* papermill_ibl.hdr */; };
@@ -34,11 +37,17 @@
 /* Begin PBXFileReference section */
 		0083126833BD37C48CC51E07 /* nn-1111cefa1111.nnue */ = {isa = PBXFileReference; path = "nn-1111cefa1111.nnue"; sourceTree = "<group>"; };
 		097ADECF758538D6C7CE71CD /* papermill_skybox.hdr */ = {isa = PBXFileReference; path = papermill_skybox.hdr; sourceTree = "<group>"; };
+		1C96BB04BA068592D857136E /* FilamentChessFactory.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = FilamentChessFactory.swift; sourceTree = "<group>"; };
 		28C7F05371BE73DC00FFB6B0 /* StockfishChessEngine.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = StockfishChessEngine.swift; sourceTree = "<group>"; };
+		2F000A1A124DCED79B34ED01 /* FilamentChessView.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = FilamentChessView.swift; sourceTree = "<group>"; };
 		39DD6E47D4950503102B28AE /* chess.glb */ = {isa = PBXFileReference; path = chess.glb; sourceTree = "<group>"; };
+		51DE2B2C91701754F7A929F8 /* FilamentChessRenderer.h */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.c.h; path = FilamentChessRenderer.h; sourceTree = "<group>"; };
 		56583BC252B5F01ED9DEA19D /* iosAppTests.xctest */ = {isa = PBXFileReference; includeInIndex = 0; lastKnownFileType = wrapper.cfbundle; path = iosAppTests.xctest; sourceTree = BUILT_PRODUCTS_DIR; };
 		668CECA83ACC03A83C85A623 /* iOSApp.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = iOSApp.swift; sourceTree = "<group>"; };
+		6F945DD39415C8D9F944D570 /* iosApp-Bridging-Header.h */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.c.h; path = "iosApp-Bridging-Header.h"; sourceTree = "<group>"; };
+		7B6D077BCA1220721132D517 /* filament.xcconfig */ = {isa = PBXFileReference; lastKnownFileType = text.xcconfig; path = filament.xcconfig; sourceTree = "<group>"; };
 		7D8DD3253426D5CAAB9B26A6 /* chess3d-host.html */ = {isa = PBXFileReference; lastKnownFileType = text.html; path = "chess3d-host.html"; sourceTree = "<group>"; };
+		84E513D241B7AC10D88E4034 /* FilamentChessRenderer.mm */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.objcpp; path = FilamentChessRenderer.mm; sourceTree = "<group>"; };
 		86038AE1032E284E79B32AD6 /* iosApp.app */ = {isa = PBXFileReference; includeInIndex = 0; lastKnownFileType = wrapper.application; path = iosApp.app; sourceTree = BUILT_PRODUCTS_DIR; };
 		9C8FE0FD78570770D4CDCE73 /* chess3d-bundle.js */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.javascript; path = "chess3d-bundle.js"; sourceTree = "<group>"; };
 		BAEBFE68677C845B5A891EB9 /* StockfishChessEngineTests.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = StockfishChessEngineTests.swift; sourceTree = "<group>"; };
@@ -67,6 +76,7 @@
 				C117AD187833E2513BD2F553 /* Info.plist */,
 				668CECA83ACC03A83C85A623 /* iOSApp.swift */,
 				28C7F05371BE73DC00FFB6B0 /* StockfishChessEngine.swift */,
+				58AE5A3F6B14DEF0F4ABBB00 /* Filament */,
 				1EEC3D4E9285A39087C3ADA0 /* Resources */,
 			);
 			path = iosApp;
@@ -103,6 +113,19 @@
 			path = iosAppTests;
 			sourceTree = "<group>";
 		};
+		58AE5A3F6B14DEF0F4ABBB00 /* Filament */ = {
+			isa = PBXGroup;
+			children = (
+				7B6D077BCA1220721132D517 /* filament.xcconfig */,
+				1C96BB04BA068592D857136E /* FilamentChessFactory.swift */,
+				51DE2B2C91701754F7A929F8 /* FilamentChessRenderer.h */,
+				84E513D241B7AC10D88E4034 /* FilamentChessRenderer.mm */,
+				2F000A1A124DCED79B34ED01 /* FilamentChessView.swift */,
+				6F945DD39415C8D9F944D570 /* iosApp-Bridging-Header.h */,
+			);
+			path = Filament;
+			sourceTree = "<group>";
+		};
 		94C98B8B3A2BFC1966447269 /* Products */ = {
 			isa = PBXGroup;
 			children = (
@@ -236,6 +259,9 @@
 			buildActionMask = 2147483647;
 			files = (
 				56B0305B5D3847FC6B326502 /* ContentView.swift in Sources */,
+				AE3AA8C5D37E58F01986EEFC /* FilamentChessFactory.swift in Sources */,
+				2C9378ABBEEF946E998A907A /* FilamentChessRenderer.mm in Sources */,
+				344EA7D190B1CBFC69C6995F /* FilamentChessView.swift in Sources */,
 				3F4B6FE98FC3A112054A4766 /* StockfishChessEngine.swift in Sources */,
 				307C2654CD5972A25FB7FD87 /* iOSApp.swift in Sources */,
 			);
@@ -434,6 +460,23 @@
 			};
 			name = Debug;
 		};
+		996E13B9A0CE2F98A99BC45F /* Debug-Filament */ = {
+			isa = XCBuildConfiguration;
+			buildSettings = {
+				BUNDLE_LOADER = "$(TEST_HOST)";
+				FRAMEWORK_SEARCH_PATHS = "$(inherited) $(SRCROOT)/../app/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)";
+				LD_RUNPATH_SEARCH_PATHS = (
+					"$(inherited)",
+					"@executable_path/Frameworks",
+					"@loader_path/Frameworks",
+				);
+				PRODUCT_BUNDLE_IDENTIFIER = com.example.myapplication.tests;
+				SDKROOT = iphoneos;
+				TARGETED_DEVICE_FAMILY = "1,2";
+				TEST_HOST = "$(BUILT_PRODUCTS_DIR)/iosApp.app/iosApp";
+			};
+			name = "Debug-Filament";
+		};
 		AF835BFF1220CA5AB5B867E4 /* Release */ = {
 			isa = XCBuildConfiguration;
 			buildSettings = {
@@ -453,6 +496,90 @@
 			};
 			name = Release;
 		};
+		E2FBA3D2BBCD59093A31DCCD /* Debug-Filament */ = {
+			isa = XCBuildConfiguration;
+			buildSettings = {
+				ALWAYS_SEARCH_USER_PATHS = NO;
+				CLANG_ANALYZER_NONNULL = YES;
+				CLANG_ANALYZER_NUMBER_OBJECT_CONVERSION = YES_AGGRESSIVE;
+				CLANG_CXX_LANGUAGE_STANDARD = "gnu++14";
+				CLANG_CXX_LIBRARY = "libc++";
+				CLANG_ENABLE_MODULES = YES;
+				CLANG_ENABLE_OBJC_ARC = YES;
+				CLANG_ENABLE_OBJC_WEAK = YES;
+				CLANG_WARN_BLOCK_CAPTURE_AUTORELEASING = YES;
+				CLANG_WARN_BOOL_CONVERSION = YES;
+				CLANG_WARN_COMMA = YES;
+				CLANG_WARN_CONSTANT_CONVERSION = YES;
+				CLANG_WARN_DEPRECATED_OBJC_IMPLEMENTATIONS = YES;
+				CLANG_WARN_DIRECT_OBJC_ISA_USAGE = YES_ERROR;
+				CLANG_WARN_DOCUMENTATION_COMMENTS = YES;
+				CLANG_WARN_EMPTY_BODY = YES;
+				CLANG_WARN_ENUM_CONVERSION = YES;
+				CLANG_WARN_INFINITE_RECURSION = YES;
+				CLANG_WARN_INT_CONVERSION = YES;
+				CLANG_WARN_NON_LITERAL_NULL_CONVERSION = YES;
+				CLANG_WARN_OBJC_IMPLICIT_RETAIN_SELF = YES;
+				CLANG_WARN_OBJC_LITERAL_CONVERSION = YES;
+				CLANG_WARN_OBJC_ROOT_CLASS = YES_ERROR;
+				CLANG_WARN_QUOTED_INCLUDE_IN_FRAMEWORK_HEADER = YES;
+				CLANG_WARN_RANGE_LOOP_ANALYSIS = YES;
+				CLANG_WARN_STRICT_PROTOTYPES = YES;
+				CLANG_WARN_SUSPICIOUS_MOVE = YES;
+				CLANG_WARN_UNGUARDED_AVAILABILITY = YES_AGGRESSIVE;
+				CLANG_WARN_UNREACHABLE_CODE = YES;
+				CLANG_WARN__DUPLICATE_METHOD_MATCH = YES;
+				COPY_PHASE_STRIP = NO;
+				DEBUG_INFORMATION_FORMAT = dwarf;
+				ENABLE_STRICT_OBJC_MSGSEND = YES;
+				ENABLE_TESTABILITY = YES;
+				ENABLE_USER_SCRIPT_SANDBOXING = NO;
+				GCC_C_LANGUAGE_STANDARD = gnu11;
+				GCC_DYNAMIC_NO_PIC = NO;
+				GCC_NO_COMMON_BLOCKS = YES;
+				GCC_OPTIMIZATION_LEVEL = 0;
+				GCC_PREPROCESSOR_DEFINITIONS = (
+					"$(inherited)",
+					"DEBUG=1",
+				);
+				GCC_WARN_64_TO_32_BIT_CONVERSION = YES;
+				GCC_WARN_ABOUT_RETURN_TYPE = YES_ERROR;
+				GCC_WARN_UNDECLARED_SELECTOR = YES;
+				GCC_WARN_UNINITIALIZED_AUTOS = YES_AGGRESSIVE;
+				GCC_WARN_UNUSED_FUNCTION = YES;
+				GCC_WARN_UNUSED_VARIABLE = YES;
+				IPHONEOS_DEPLOYMENT_TARGET = 16.0;
+				MTL_ENABLE_DEBUG_INFO = INCLUDE_SOURCE;
+				MTL_FAST_MATH = YES;
+				ONLY_ACTIVE_ARCH = YES;
+				PRODUCT_NAME = "$(TARGET_NAME)";
+				SDKROOT = iphoneos;
+				SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG;
+				SWIFT_OPTIMIZATION_LEVEL = "-Onone";
+				SWIFT_VERSION = 5.0;
+			};
+			name = "Debug-Filament";
+		};
+		E552362FA76B7DB3AAAB9122 /* Debug-Filament */ = {
+			isa = XCBuildConfiguration;
+			baseConfigurationReference = 7B6D077BCA1220721132D517 /* filament.xcconfig */;
+			buildSettings = {
+				ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon;
+				CODE_SIGN_IDENTITY = "iPhone Developer";
+				CODE_SIGN_STYLE = Automatic;
+				FRAMEWORK_SEARCH_PATHS = "$(inherited) $(SRCROOT)/../app/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)";
+				INFOPLIST_FILE = iosApp/Info.plist;
+				LD_RUNPATH_SEARCH_PATHS = (
+					"$(inherited)",
+					"@executable_path/Frameworks",
+				);
+				OTHER_LDFLAGS = "$(inherited) -framework ChessApp";
+				PRODUCT_BUNDLE_IDENTIFIER = com.example.myapplication;
+				SDKROOT = iphoneos;
+				TARGETED_DEVICE_FAMILY = "1,2";
+			};
+			name = "Debug-Filament";
+		};
 /* End XCBuildConfiguration section */
 
 /* Begin XCConfigurationList section */
@@ -460,6 +587,7 @@
 			isa = XCConfigurationList;
 			buildConfigurations = (
 				9221A1FDC06803E8A2FC6E61 /* Debug */,
+				E552362FA76B7DB3AAAB9122 /* Debug-Filament */,
 				AF835BFF1220CA5AB5B867E4 /* Release */,
 			);
 			defaultConfigurationIsVisible = 0;
@@ -469,6 +597,7 @@
 			isa = XCConfigurationList;
 			buildConfigurations = (
 				24229CBC853481EF040BD930 /* Debug */,
+				996E13B9A0CE2F98A99BC45F /* Debug-Filament */,
 				658C518871404178F6E62E6C /* Release */,
 			);
 			defaultConfigurationIsVisible = 0;
@@ -478,6 +607,7 @@
 			isa = XCConfigurationList;
 			buildConfigurations = (
 				6C824A6EB4BAD43AE2A4BB29 /* Debug */,
+				E2FBA3D2BBCD59093A31DCCD /* Debug-Filament */,
 				11814011E7D6EC822BFBC66D /* Release */,
 			);
 			defaultConfigurationIsVisible = 0;
diff --git a/iosApp/iosApp.xcodeproj/project.xcworkspace/xcuserdata/presence.xcuserdatad/UserInterfaceState.xcuserstate b/iosApp/iosApp.xcodeproj/project.xcworkspace/xcuserdata/presence.xcuserdatad/UserInterfaceState.xcuserstate
index d69d4a9..0abc8e0 100644
Binary files a/iosApp/iosApp.xcodeproj/project.xcworkspace/xcuserdata/presence.xcuserdatad/UserInterfaceState.xcuserstate and b/iosApp/iosApp.xcodeproj/project.xcworkspace/xcuserdata/presence.xcuserdatad/UserInterfaceState.xcuserstate differ
diff --git a/iosApp/iosApp/ContentView.swift b/iosApp/iosApp/ContentView.swift
index 91e7051..5f2e474 100644
--- a/iosApp/iosApp/ContentView.swift
+++ b/iosApp/iosApp/ContentView.swift
@@ -4,7 +4,21 @@ import ChessApp
 
 struct ComposeView: UIViewControllerRepresentable {
     func makeUIViewController(context: Context) -> UIViewController {
-        MainViewControllerKt.MainViewController(engine: StockfishChessEngine())
+        // 3D board backend selection (issue #54):
+        //   - default build: filamentFactory = nil -> three.js/WKWebView renderer (shipping path).
+        //   - Debug-Filament build (FILAMENT_ENABLED): Metal-native Filament renderer, unless the
+        //     CHESS_IOS_WEBVIEW env var forces the WKWebView fallback at runtime.
+        // See docs/plans/ios-filament-spike-result.md.
+        var filamentFactory: FilamentChessViewFactory? = nil
+        #if FILAMENT_ENABLED
+        if ProcessInfo.processInfo.environment["CHESS_IOS_WEBVIEW"] == nil {
+            filamentFactory = FilamentChessFactory()
+        }
+        #endif
+        return MainViewControllerKt.MainViewController(
+            engine: StockfishChessEngine(),
+            filamentFactory: filamentFactory
+        )
     }
     func updateUIViewController(_ uiViewController: UIViewController, context: Context) {}
 }
diff --git a/iosApp/project.yml b/iosApp/project.yml
index 3ac132f..daf2a87 100644
--- a/iosApp/project.yml
+++ b/iosApp/project.yml
@@ -4,6 +4,16 @@ options:
   deploymentTarget:
     iOS: "16.0"          # floor set by ChessKitEngine (.iOS(.v16))
   createIntermediateGroups: true
+# Debug-Filament (issue #54) is an optional config that builds the Metal-native Filament 3D renderer
+# instead of the default three.js/WKWebView path. The $(CONFIGURATION)-templated FRAMEWORK_SEARCH_PATHS
+# below resolves the Kotlin framework under app/build/xcode-frameworks/Debug-Filament/; the Kotlin embed
+# task can't auto-detect the build type for a non-Debug/Release config name, so filament.xcconfig sets
+# KOTLIN_FRAMEWORK_BUILD_TYPE=debug. Default Debug/Release are unchanged. Requires
+# tools/fetch_filament_ios.sh first; see docs/plans/ios-filament-spike-result.md.
+configs:
+  Debug: debug
+  Release: release
+  Debug-Filament: debug
 settings:
   base:
     ENABLE_USER_SCRIPT_SANDBOXING: "NO"   # required by embedAndSignAppleFrameworkForXcode
@@ -17,9 +27,11 @@ targets:
     platform: iOS
     sources:
       - path: iosApp
-        excludes: ["Resources/**"]
+        # Filament/filament/** is the fetched Filament release (headers + xcframeworks); it is linked
+        # via filament.xcconfig, not compiled as target sources, so keep xcodegen from globbing it.
+        excludes: ["Resources/**", "Filament/filament/**"]
       - path: iosApp/Resources
-        buildPhase: resources          # .nnue files must land in the app bundle
+        buildPhase: resources          # .nnue + KTX/glb assets must land in the app bundle
     dependencies:
       - package: ChessKitEngine
     info:
@@ -36,6 +48,11 @@ targets:
         OTHER_LDFLAGS: "$(inherited) -framework ChessApp"
         TARGETED_DEVICE_FAMILY: "1,2"
         CODE_SIGN_STYLE: Automatic
+    # All Filament-specific settings (FILAMENT_ENABLED flag, bridging header, header/library search
+    # paths, static-lib link set) live in this xcconfig, applied only to Debug-Filament so the default
+    # configs stay clean. The xcconfig is committed; the libraries it points at are fetched separately.
+    configFiles:
+      Debug-Filament: iosApp/Filament/filament.xcconfig
     preBuildScripts:
       - name: Compile Kotlin Framework
         basedOnDependencyAnalysis: false
@@ -64,3 +81,13 @@ schemes:
     test:
       config: Debug
       targets: [iosAppTests]
+  # Builds/runs the Metal-native Filament 3D renderer (issue #54). Requires the Filament libraries
+  # (tools/fetch_filament_ios.sh) to be present, otherwise the link step fails.
+  iosApp-Filament:
+    build:
+      targets: { iosApp: all }
+    run:
+      config: Debug-Filament
+    test:
+      config: Debug-Filament
+      targets: [iosAppTests]
